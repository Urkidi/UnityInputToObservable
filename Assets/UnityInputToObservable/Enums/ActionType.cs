using UnityInputToObservable.Utils;

namespace UnityInputToObservable.Enums
{
    public enum ActionType
    {
        [StringRepresentation("Move")] Move = 1,
        [StringRepresentation("Action")] Action = 2,
    }
}